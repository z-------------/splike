{
    "scripts": {
        "gen": "pegjs -o commentsParser.js comments.pegjs && pegjs -o grammarParser.js grammar.pegjs",
        "run": "node interpreter/main",
        "repl": "node repl/main"
    },
    "dependencies": {
        "pegjs": "^0.10.0"
    }
}
