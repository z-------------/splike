{
    "scripts": {
        "gen": "pegjs -o commentsParser.js grammar/comments.pegjs && pegjs -o grammarParser.js grammar/grammar.pegjs",
        "run": "node interpreter/main",
        "repl": "node repl/main"
    },
    "dependencies": {
        "pegjs": "^0.10.0"
    }
}
